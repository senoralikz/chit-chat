import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SelectChatScreen = ({ navigation }) => {
  return (
    <View>
      <Text>SelectChatScreen</Text>
    </View>
  );
};

export default SelectChatScreen;

const styles = StyleSheet.create({});
